description: Successfully changed user password
content:
    application/json:
        schema:
            type: object
            properties:
                message:
                    type: string
                    example: Password for user 'admin' changed successfully
