description: New password
required: true
content:
    application/json:
        schema:
            type: object
            properties:
                secret:
                    type: newPassword
                    description: The new password for the user.
