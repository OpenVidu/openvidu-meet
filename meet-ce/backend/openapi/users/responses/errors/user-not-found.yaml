description: User not found
content:
    application/json:
        schema:
            $ref: '../../../shared/schemas/error.yaml'
        example:
            error: 'User Error'
            message: 'User "alice_smith" not found'
