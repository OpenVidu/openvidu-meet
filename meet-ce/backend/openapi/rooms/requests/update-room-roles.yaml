description: Room roles permissions update options
required: true
content:
    application/json:
        schema:
            type: object
            properties:
                roles:
                    $ref: '../schemas/meet-room-roles-config.yaml'
            required:
                - roles
