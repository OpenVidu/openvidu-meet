description: New room config
required: true
content:
    application/json:
        schema:
            type: object
            properties:
                config:
                    $ref: '../schemas/meet-room-config.yaml#/MeetRoomConfig'
            required:
                - config
        example:
            config:
                recording:
                    enabled: true
                    encoding: H264_720P_30
                chat:
                    enabled: true
                virtualBackground:
                    enabled: true
