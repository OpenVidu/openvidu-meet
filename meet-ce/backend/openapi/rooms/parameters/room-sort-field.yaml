name: sortField
in: query
required: false
description: The room field by which to sort the results.
schema:
    type: string
    enum:
        - creationDate
        - roomName
        - autoDeletionDate
