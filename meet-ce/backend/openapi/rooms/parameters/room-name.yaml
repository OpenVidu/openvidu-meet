name: roomName
in: query
required: false
description: >
    Filter rooms by name.
    By default, roomName uses exact and case-sensitive matching.
    Use roomNameMatchMode and roomNameCaseInsensitive to customize matching behavior.
schema:
    type: string
example: 'room'
