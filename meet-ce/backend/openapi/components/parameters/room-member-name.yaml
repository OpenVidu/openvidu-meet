name: name
in: query
required: false
description: >
    Filter members by name.
    By default, name uses exact and case-sensitive matching.
    Use nameMatchMode and nameCaseInsensitive to customize matching behavior.
schema:
    type: string
example: 'Alice'
