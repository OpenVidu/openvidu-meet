name: status
in: query
required: false
description: Filter recordings by their status.
schema:
    type: string
    enum:
        - starting
        - active
        - ending
        - complete
        - failed
        - aborted
        - limit_reached
