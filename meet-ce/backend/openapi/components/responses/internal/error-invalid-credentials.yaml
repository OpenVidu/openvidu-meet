description: Invalid credentials
content:
    application/json:
        schema:
            $ref: ../../schemas/error.yaml
        example:
            error:  Login Error
            message: 'Invalid username or password'
