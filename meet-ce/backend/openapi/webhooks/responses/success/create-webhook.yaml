description: The registered webhook
content:
    application/json:
        schema:
            $ref: '../../schemas/meet-webhook-endpoint.yaml'
headers:
    Location:
        description: URL of the newly registered webhook
        schema:
            type: string
            format: uri
        example: https://example.com/api/v1/webhooks/wh-a1b2c3d4e5f6g7h
