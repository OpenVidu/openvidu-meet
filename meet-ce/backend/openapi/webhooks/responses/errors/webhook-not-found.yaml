description: Webhook not found
content:
    application/json:
        schema:
            $ref: '../../../shared/schemas/error.yaml'
        example:
            error: 'Webhook Error'
            message: "Webhook 'wh-a1b2c3d4e5f6g7h' does not exist"
