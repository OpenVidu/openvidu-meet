description: Invalid credentials
content:
    application/json:
        schema:
            $ref: ../../../shared/schemas/error.yaml
        example:
            error:  Login Error
            message: 'Invalid user ID or password'
