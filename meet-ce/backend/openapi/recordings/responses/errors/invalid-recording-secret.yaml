description: Invalid recording secret
content:
    application/json:
        schema:
            $ref: ../../../shared/schemas/error.yaml
        example:
            error: 'Recording Error'
            message: "Provided secret is not recognized for recording 'room-123--EG_XYZ--XX445'"