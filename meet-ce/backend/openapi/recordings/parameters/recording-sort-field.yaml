name: sortField
in: query
required: false
description: The recording field by which to sort the results.
schema:
    type: string
    enum:
        - startDate
        - roomName
        - duration
        - size
