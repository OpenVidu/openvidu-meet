description: Successfully tested webhook URL
content:
    application/json:
        schema:
            type: object
            properties:
                message:
                    type: string
                    example: 'Webhook URL is valid'
