description: Successfully updated rooms appearance config. Returns the updated appearance configuration.
content:
    application/json:
        schema:
            $ref: '../../schemas/rooms-appearance-config.yaml'
