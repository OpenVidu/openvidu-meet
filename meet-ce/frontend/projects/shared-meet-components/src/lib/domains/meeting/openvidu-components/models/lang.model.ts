// The i18n model types now live in the shared layer so both the meeting and the console can reuse
// them. Re-exported here to keep the existing `openvidu-components` import paths stable.
export * from '../../../../shared/models/lang.model';
